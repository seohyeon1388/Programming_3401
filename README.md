# Programming 소스코드

### 02_counter

vanilla HTML, JavaScript

## Reate 시작

```shell
npm create vite@latest .
```

- `useState(초기값)`
- `onClick={()=>set함수(바꿀값)}`
- `onClick={()=>set함수(이전 state)=>이전state+1}`
- `onClick={plusMax10}`

### CounterApp
